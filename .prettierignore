# Prettier does not read .gitignore, so build output has to be listed here too.
**/node_modules/
**/.next/
**/dist/
**/out/
**/build/
**/coverage/
**/.turbo/
**/test-results/
**/playwright-report/

# Git worktrees are full copies of this repo; formatting them would rewrite
# files that belong to another checkout.
.worktrees/
**/.claude/worktrees/

# Owned by pnpm.
pnpm-lock.yaml

# Vendored verbatim from upstream at a pinned commit — see
# apps/web/lib/foliate-js/VENDOR.md, which documents re-vendoring as "copy the
# files listed above verbatim". Reformatting them would make every future
# upstream diff unreadable and add a re-apply step to each re-vendor. These
# files are already excluded from ESLint for the same reason.
apps/web/lib/foliate-js/

# Generated by drizzle-kit: the .sql migrations and the meta/*.json snapshots it
# diffs them against. Both are rewritten by `drizzle-kit generate`.
apps/backend/drizzle/

# Generated by Next.js on build.
**/next-env.d.ts

# Static assets that happen to be parseable.
apps/web/public/
apps/website/public/
